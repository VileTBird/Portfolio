<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Library</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #0F0F0F;
            color: #FFFFFF;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            color: #FFD700;
        }

        .project-list {
            width: 100%;
            
            margin: 0 auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 255, 0, 0.5);
        }

        .project-item {
            padding: 20px;
            color: #FFFFFF;
            border-radius: 10px;
            margin-bottom: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.7);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
        }

        .project-item:hover {
            transform: translateY(-8px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.9);
        }

        .project-item h5 {
            margin-bottom: 15px;
            color: #FFD700;
            font-size: 24px;
            border-bottom: 2px solid #FFD700;
            padding-bottom: 5px;
        }

        .project-item p {
            margin-bottom: 15px;
            color: #B0B0B0;
            font-size: 16px;
            line-height: 1.8;
        }

        .project-item a {
            color: #FFD700;
            text-decoration: none;
            font-weight: bold;
            margin-top: 10px;
            display: block;
            transition: color 0.3s ease;
        }

        .project-item a:hover {
            color: #FFA500;
        }

        .status, .rating {
            font-weight: bold;
            display: inline-block;
            margin-right: 15px;
        }

        @media (max-width: 768px) {
            .project-list {
                width: 100%;
            }
        }
    </style>
</head>

<body>
    

    {{#if user}}
    <a href="media/create" style="display: block; text-align: center; color: #FFD700; margin: 20px;">Add new Project</a>
    {{/if}}


    <div class="project-list">
        <h3 style="text-align: left;margin-bottom: 10px;
    border-bottom: 2px solid #FFD700;">Project Library</h3>
    <p style="text-align: left; color: #FFD700; padding: 0 20px;">
        Just clicking on the project title will take you to the project repo! 
        (some repos are private right now but will be changed to public in the near future)
    </p>
        {{#each media}}
        <div class="project-item">
            <h5><a href="{{this.provider}}">{{this.title}}</a></h5>
            <p>Type: {{this.type}}</p>
            <p>{{this.description}}</p>
            <p class="status">Status: {{this.status}}</p>
            <p class="rating">Difficulty rating: {{this.rating}}/5</p>

            {{#if ../user}}
            <a href="/media/edit/{{this._id}}" class="btn btn-primary">Edit</a>
            <a href="/media/delete/{{this._id}}" class="btn btn-primary" onclick="return confirm('For real??');">Delete</a>
            {{/if}}
        </div>
        {{/each}}
    </div>
</body>

</html>