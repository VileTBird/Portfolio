<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Library</title>
    <style>
        /* General Body Styles */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #000000;
            color: #FFFFFF;
            margin: 0;
            padding: 0;
        }

        /* Heading Styles */
        h1 {
            text-align: center;
            color: #FFD700;
            margin: 20px 0;
        }

        /* Card Container Styles */
        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            padding: 20px;
        }

        /* Card Styles */
        .card {
            padding: 20px;
            background-color: #1a1a1a;
            color: #FFD700;
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            position: relative;
        }

        /* Hover Effect */
        .card:hover {
            transform: scale(1.05);
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.3);
        }

        /* Title and Text Styles */
        .card h5 {
            margin-bottom: 10px;
        }

        .card p {
            margin-bottom: 10px;
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
        }

        /* Link Styles */
        .card a {
            color: #FFD700;
            text-decoration: none;
            font-weight: bold;
        }

        .card-link {
            text-decoration: none;
            color: inherit;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .card-container {
                grid-template-columns: 1fr;
            }

            .card {
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <h1>Project Library</h1>
    <!-- Add New Project Link -->
    {{#if user}}
    <a href="media/create" style="display: block; text-align: center; color: #FFD700; margin: 20px;">Add new Project</a>
    {{/if}}

    <!-- Description Paragraph -->
    <p style="text-align: center; color: #FFD700; padding: 0 20px;">Just clicking on the project card will take you to the project repo! (some repos are priv rn but will be changed to public in the near future)</p>

    <!-- Card Container -->
    <div class="card-container">
        {{#each media}}
        <a href="{{this.provider}}" class="card-link">
            <div class="card">
                <h5>{{this.title}}</h5>
                <h6>{{this.type}}</h6>
                <p>{{this.description}}</p>
                <p>{{this.status}}</p>
                <p>Difficulty rating: {{this.rating}}/5</p>
                {{#if ../user}}
                <a href="/media/edit/{{this._id}}" class="btn btn-primary" style="display: block; margin: 10px auto; color: #FFD700;">Edit</a>
                <a href="/media/delete/{{this._id}}" class="btn btn-primary" style="display: block; margin: 10px auto; color: #FFD700;" onclick="return confirm('For real??');">Delete</a>
                {{/if}}
            </div>
        </a>
        {{/each}}
    </div>
</body>

</html>
