<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Library</title>
    <style>
        /* General Body Styles */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #1E1E1E;
            color: #FFFFFF;
            margin: 0;
            padding: 0;
        }

        /* Heading Styles */
        h1 {
            text-align: center;
            color: white;
            margin: 20px 0;
        }

        /* Project List Styles */
        .project-list {
            width: 90%;
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Individual Project Styles */
        .project-item {
            padding: 15px;
            background-color: #1a1a1a;
            color: white;
            border-radius: 10px;
            margin-bottom: 20px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        /* Hover Effect */
        .project-item:hover {
            transform: translateY(-5px);
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.3);
        }

        /* Title and Text Styles */
        .project-item h5 {
            margin-bottom: 10px;
            color: #FFD700;
        }

        .project-item p {
            margin-bottom: 10px;
            color: #B0B0B0;
        }

        /* Link Styles */
        .project-item a {
            color: #FFD700;
            text-decoration: none;
            font-weight: bold;
            display: block;
            margin-top: 10px;
        }

        /* Status and Difficulty Styles */
        .status, .rating {
            display: inline-block;
            margin-right: 15px;
            font-weight: bold;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .project-list {
                width: 100%;
            }
        }

    </style>
</head>

<body>
    <h1>Project Library</h1>
    
    <!-- Add New Project Link -->
    {{#if user}}
    <a href="media/create" style="display: block; text-align: center; color: #FFD700; margin: 20px;">Add new Project</a>
    {{/if}}

    <!-- Description Paragraph -->
    <p style="text-align: center; color: #FFD700; padding: 0 20px;">
        Just clicking on the project title will take you to the project repo! 
        (some repos are private right now but will be changed to public in the near future)
    </p>

    <!-- Project List -->
    <div class="project-list">
        {{#each media}}
        <div class="project-item">
            <h5><a href="{{this.provider}}">{{this.title}}</a></h5>
            <p>Type: {{this.type}}</p>
            <p>{{this.description}}</p>
            <p class="status">Status: {{this.status}}</p>
            <p class="rating">Difficulty rating: {{this.rating}}/5</p>

            {{#if ../user}}
            <a href="/media/edit/{{this._id}}" class="btn btn-primary" style="color: #FFD700;">Edit</a>
            <a href="/media/delete/{{this._id}}" class="btn btn-primary" style="color: #FFD700;" onclick="return confirm('For real??');">Delete</a>
            {{/if}}
        </div>
        {{/each}}
    </div>
</body>

</html>
